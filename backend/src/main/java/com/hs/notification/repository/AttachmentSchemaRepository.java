package com.hs.notification.repository;

import com.hs.notification.model.AttachmentSchema;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AttachmentSchemaRepository extends JpaRepository<AttachmentSchema, Long> {
}
